{
    "name": "@margwa/database",
    "version": "1.0.0",
    "description": "Shared database schema and utilities for Margwa backend",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "db:generate": "drizzle-kit generate:pg",
        "db:migrate": "npx tsx scripts/migrate.ts",
        "db:push": "drizzle-kit push:pg",
        "db:studio": "drizzle-kit studio"
    },
    "dependencies": {
        "drizzle-orm": "^0.29.1",
        "postgres": "^3.4.3"
    },
    "devDependencies": {
        "@types/node": "^20.10.0",
        "drizzle-kit": "^0.20.18",
        "typescript": "^5.3.3"
    }
}
